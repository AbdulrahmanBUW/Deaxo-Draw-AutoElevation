<Window x:Class="Deaxo.AutoElevation.UI.SelectFromDictWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DEAXO Auto Elevation - Template Selection"
        Height="700" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA"
        FontFamily="Segoe UI"
        ResizeMode="CanResize"
        MinHeight="500" MinWidth="700">

    <Window.Resources>
        <!-- Modern Card Container -->
        <Style x:Key="MainCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="32"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" Direction="270" ShadowDepth="8" Opacity="0.1"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Enhanced Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="10"
                                Padding="20,10"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary Button Style -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="BorderBrush" Value="#DEE2E6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E9ECEF"/>
                    <Setter Property="BorderBrush" Value="#ADB5BD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Enhanced RadioButton Style for Single Selection -->
        <Style x:Key="ModernRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="#FFFFFF"
                                CornerRadius="10"
                                Padding="16,12"
                                Margin="4,2"
                                BorderBrush="#E9ECEF"
                                BorderThickness="1"
                                SnapsToDevicePixels="True">
                            <StackPanel Orientation="Horizontal">
                                <Border x:Name="radioBorder" 
                                        Width="20" Height="20" 
                                        CornerRadius="10"
                                        BorderBrush="#CED4DA" 
                                        BorderThickness="2"
                                        Background="White"
                                        Margin="0,0,12,0">
                                    <Ellipse x:Name="radioMark"
                                             Width="8" Height="8"
                                             Fill="White"
                                             Visibility="Collapsed"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
                                </Border>
                                <ContentPresenter VerticalAlignment="Center" 
                                                TextBlock.FontSize="14"
                                                TextBlock.FontWeight="Medium"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F8F9FA"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#007AFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E3F2FD"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#007AFF"/>
                                <Setter TargetName="radioBorder" Property="Background" Value="#007AFF"/>
                                <Setter TargetName="radioBorder" Property="BorderBrush" Value="#007AFF"/>
                                <Setter TargetName="radioMark" Property="Visibility" Value="Visible"/>
                                <Setter Property="Foreground" Value="#007AFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Fixed Search TextBox Style - Based on working BaseNameInputWindow -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Foreground" Value="#495057"/>
            <Setter Property="CaretBrush" Value="#007AFF"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Focusable="false"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#007AFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Header Style -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#212529"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- Subheader Style -->
        <Style x:Key="SubheaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#6C757D"/>
            <Setter Property="LineHeight" Value="20"/>
        </Style>
    </Window.Resources>

    <!-- Main Layout -->
    <Border Style="{StaticResource MainCardStyle}" Margin="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Brand Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24" HorizontalAlignment="Center">
                <Border Background="#007AFF" CornerRadius="6" Padding="12,6" Margin="0,0,0,16" HorizontalAlignment="Center">
                    <TextBlock Text="DEAXO" FontSize="14" FontWeight="Bold" Foreground="White"/>
                </Border>
                <TextBlock x:Name="TitleText" Style="{StaticResource HeaderStyle}" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Instructions and Search -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Instructions -->
                <StackPanel Grid.Row="0" Margin="0,0,0,22">
                    <TextBlock Text="Select a view template to apply to your elevation views:" 
                               Style="{StaticResource SubheaderStyle}" 
                               TextWrapping="Wrap"
                               Margin="0,0,0,8"/>
                </StackPanel>

                <!-- Search Box -->
                <StackPanel Grid.Row="1">
                    <TextBlock Text="Search Templates:" FontSize="12" FontWeight="Medium" Foreground="#495057" Margin="0,0,0,8"/>
                    <TextBox x:Name="SearchBox" 
                             Style="{StaticResource SearchBoxStyle}"
                             TextChanged="SearchBox_TextChanged"
                             GotFocus="SearchBox_GotFocus"
                             LostFocus="SearchBox_LostFocus"/>
                </StackPanel>
            </Grid>

            <!-- Template List -->
            <Border Grid.Row="2" 
                    Background="#FAFBFC" 
                    CornerRadius="8" 
                    BorderBrush="#E9ECEF" 
                    BorderThickness="1"
                    Padding="8"
                    Margin="0,16,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" 
                              HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl x:Name="OptionsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <RadioButton Content="{Binding}" 
                                            Style="{StaticResource ModernRadioButtonStyle}"
                                            GroupName="TemplateSelection"
                                            Margin="4"
                                            Checked="RadioButton_Checked"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <!-- Action Buttons -->
            <Grid Grid.Row="3" Margin="0,24,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Status Text -->
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock x:Name="StatusText" 
                               Text="Select a template or continue with default formatting" 
                               FontSize="12" 
                               Foreground="#6C757D"/>
                </StackPanel>

                <!-- Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Cancel" 
                            Width="100" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Margin="0,0,12,0"
                            Click="Cancel_Click"/>
                    <Button Content="Continue" 
                            Width="120" 
                            Background="#007AFF" 
                            Foreground="White"
                            Style="{StaticResource ModernButtonStyle}"
                            Click="Ok_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>